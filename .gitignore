# Generated by build.py — the site is built in CI and published to GitHub Pages,
# so the artefact is no longer committed. Regenerate locally with `python3 build.py`.
marknad_tent_planner.html

# build.py syntax-check scratch file
.check.js

# local preview server dir
_site/

# python bytecode cache (e.g. from importing extract_tents.py)
__pycache__/
*.pyc
